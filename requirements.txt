# Streamlit Community Cloud deployment requirements.
# Pinning the CPU-only PyTorch index avoids pulling triton + CUDA wheels,
# which is what broke the previous Python 3.14 build.
# For local development, prefer:  pip install -e ".[app,dev]"
--extra-index-url https://download.pytorch.org/whl/cpu

numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
xgboost>=2.0
shap>=0.44
torch>=2.1
statsmodels>=0.14
joblib>=1.3
tqdm>=4.66
streamlit>=1.30
plotly>=5.18

# Install the project itself so `import sensorlab` works on Streamlit Cloud.
-e .
